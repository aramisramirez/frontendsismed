const keyGlobal = {
  url: 'https://localhost:7299/api/'
}

export  const keyauth = {
  login: keyGlobal.url + 'auth/login'
}

export const keygetusuario = {
  usuario: keyGlobal.url + 'persona/obtener/Activos'
}
